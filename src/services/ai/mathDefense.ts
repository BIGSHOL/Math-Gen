/**
 * 수학 풀이 방어 프롬프트 — 학년별 fragment 모듈.
 *
 * **배경**: 사용자가 인재원(IJW) 의 9 섹션 방어 프롬프트 문서를 제공
 * (`C:\Users\user\Downloads\math-defense-prompt.md`). 패턴 A-H 메타 인지
 * 오류 + 자가 검증 8단계 + 학교급별 단원별 함정 (~570 줄). 전체를 매
 * 해설 호출마다 inject 하면 token 폭발 — 학년별 fragment 로 쪼개서 *해당
 * 학년만* 동적으로 합쳐서 사용.
 *
 * **사용 방법**:
 *   const defense = buildMathDefense(selectedGrade);
 *   const prompt = SOLUTION_PROMPT.replace("{mathDefense}", defense);
 *
 * `buildMathDefense(null)` 은 공통 fragment 만 반환 — 학년 미선택 시 fallback.
 *
 * **변환 정책** (mathlab `ExamUploadForm.tsx` 벤치마킹):
 * - mathlab 은 select 드롭다운 (학년 6개 + 카테고리 세부). 우리는 fragment 가
 *   10 개라 더 세밀 — 학년 + 과목 조합을 fragment key 로 변환.
 * - Python brute-force 검증 → "머릿속 simulation" 으로 변환 (우리 환경에 코드 실행 X).
 * - 서술형 답안 5 단계 명시는 우리 HARD CAP·보일러플레이트 금지와 충돌 → 미적용.
 * - 학생 수준별 강의식 설명 (§6) — 우리는 해설 생성만 → 미적용.
 *
 * **CLAUDE.md 4-6 함정 주의**: 이 파일은 큰 template literal 을 다루므로
 * backtick 의 escape (\`) 와 unicode 인용부호 사용에 신경 쓸 것.
 */

/** 학년 + 과목 결합 fragment 키 (10 개). */
export type GradeKey =
  | "middle1"
  | "middle2"
  | "middle3"
  | "high1_common1"
  | "high1_common2"
  | "high2_algebra"
  | "high2_calc1"
  | "high2_stats"
  | "high2_geometry"
  | "high3_calc2";

/** UI 표시용 한국어 라벨. */
export const GRADE_LABELS: Record<GradeKey, string> = {
  middle1: "중1",
  middle2: "중2",
  middle3: "중3",
  high1_common1: "고1 공통수학1",
  high1_common2: "고1 공통수학2",
  high2_algebra: "고2 대수",
  high2_calc1: "고2 미적분I",
  high2_stats: "고2 확률과 통계",
  high2_geometry: "고2 기하",
  high3_calc2: "고3 미적분II",
};

/** UI chip group 순서. 그룹별 묶음 — 중등 / 고1 / 고2 / 고3. */
export const GRADE_GROUPS: Array<{ label: string; items: GradeKey[] }> = [
  { label: "중등", items: ["middle1", "middle2", "middle3"] },
  { label: "고1", items: ["high1_common1", "high1_common2"] },
  { label: "고2", items: ["high2_algebra", "high2_calc1", "high2_stats", "high2_geometry"] },
  { label: "고3", items: ["high3_calc2"] },
];

// ──────────────────────────────────────────────────────────────────
// 공통 fragment — 모든 학년에 inject. 패턴 A-H + 8단계 + 최우선 원칙.
// ──────────────────────────────────────────────────────────────────

export const MATH_DEFENSE_COMMON = `🚨🚨🚨 **수학적 검증 절차 — 답 확정 전 반드시 수행 (사용자 반복 보고, CRITICAL)**

**원칙**: "후보를 좁히기 전에, 후보를 끝까지 펼쳐라."

다음 8 가지 메타 인지 오류 패턴을 *답을 확정하기 전에* 의식적으로 점검할 것:

─── **오류 패턴 A: "필수" 와 "상한" 을 혼동 (max-condition trap)** ───

최소공배수·\\max(A, B) = C 형태의 조건에서, "B = C 여야 한다" 고 단정하지 말 것. A ≥ C 이면 B 의 값은 0 부터 C 까지 **자유롭게 가능**.

잘못된 사례 (사용자 보고 13 번 — 실제 모델이 emit 한 풀이):
  문제: "두 자연수 324 와 21 × □ 의 최소공배수가 2² × 3⁴ × 7 일 때, □ 안에 들어갈 수 있는 모든 자연수의 합."
  잘못된 풀이: "□ 는 반드시 2² 을 포함해야 한다 → □ = 4 × 3^k"
  → **틀림!** 324 = 2² × 3⁴ 이 이미 2² 을 갖고 있으므로 □ 안의 2 의 지수는 0, 1, 2 모두 가능.
  → 누락된 case 들: □ = 1, 2, 3, 6, 9, 12, 18, 27, ... (3 의 지수도 0..4 다 가능).

올바른 검증 절차:
  1. 어떤 인수 p^k 에 대해 "□ 는 반드시 p^c 포함" 이라 단정하기 전에, *다른 수에 그 인수가 이미 c 이상으로 있는지* 확인.
  2. 있다면 □ 의 p 지수는 "선택" — 0, 1, ..., c 모두 가능.
  3. 없을 때만 "필수" — □ 의 p 지수가 정확히 c 여야 함.

적용 단원: 최대공약수/최소공배수, 부등식, 함수의 정의역, 집합의 포함관계.

─── **오류 패턴 B: 제약 조건을 잘못된 단계에 적용 (constraint-stage trap)** ───

"서로 다른 X" 같은 조건은 **최종 결과물** 에 대한 조건. 중간 계산 (절댓값, 약수 분해, 인수 집합 등) 에 미리 적용해서 case 를 *조기 제외* 하지 말 것.

잘못된 사례 (사용자 보고 — 실제 모델이 emit 한 풀이):
  문제: "서로 다른 세 정수의 곱 = -50. 가능한 세 정수의 합을 모두 구하라."
  잘못된 풀이: "절댓값 분해 2 × 5 × 5 는 5 가 중복이므로 *불가*"
  → **틀림!** 절댓값에서 {2, 5, 5} 가 중복이어도, 부호 배정 단계에서 (-5, 2, 5) 가 되면 *세 정수는 서로 다름*. 절댓값 중복 ≠ 정수 중복.
  → 누락된 합: (-5) + 2 + 5 = 2 같은 case.

올바른 검증 절차:
  1. "이 제약은 *어느 단계* 에 대한 조건인가?" 자문.
  2. *최종 결과물* (정수 자체) 조건이면 중간 단계 (절댓값) 중복은 OK — case 살려야 함.
  3. *각 단계* 조건이면 중간에서도 적용.

적용 단원: 정수와 유리수, 집합, 함수의 일대일 조건, 조합 문제.

─── **오류 패턴 J: 부호 배정 후 정수 중복 검증 누락 (set-distinct post-assignment, CRITICAL)** ───

증상: 절댓값 분해에 *중복 절댓값* 이 있는 조합 (예: {1, 1, 50}) 에서 부호 배정 후 *원소 자체가 동일한* case 를 *유효* 로 잘못 카운트. Pattern B (조기 제외 금지) 의 *역방향 함정* — case 를 살려놓고도 *부호 배정 후* 동일 정수가 둘 들어가는지 안 봄.

잘못된 사례 (사용자 보고 — 실제 모델이 emit 한 풀이):
  문제: "서로 다른 세 정수의 곱 = -50. 가능한 합을 모두 구하라."
  잘못된 풀이:
    "{1, 1, 50}: 음수 1개 배정 → (−1, 1, 50): 합 50, (−50, 1, 1): 합 −48..."
  → **틀림!** (−50, 1, 1) 은 정수 1 이 *두 번* 들어가 *서로 다름 위반*. -48 은 invalid case.
  → 모델 emit 정답: -48 포함 → 11 개. 진짜 정답: -48 제외 → 10 개.

핵심 룰 (절댓값에 중복 있을 때 부호 배정 case 검증):
  절댓값 조합 {a, a, b} (a 중복) 에서 부호 배정 후 *유효 케이스*:
    1. **a 두 개에 *서로 다른 부호*** → (a, -a, b) 또는 (-a, a, b): 정수 a 와 -a 는 서로 다름. ✓ 유효.
    2. **a 두 개에 *같은 부호***, b 만 다른 부호 → (a, a, -b) 또는 (-a, -a, b): 정수 a 가 *두 번* → 서로 다름 위반. ✗ 무효.
    3. **모두 같은 부호** → (a, a, b) 또는 (-a, -a, -b): a 가 *두 번* → 무효.
  → 절댓값 중복이 *둘* 인 조합은 case 의 2/4 만 유효 (a/-a 분리 + b 한 쪽).
  → 절댓값 중복이 *셋* 인 조합 (예: {a, a, a}) 은 *모든 부호 배정 무효*.

올바른 검증 절차 (각 case 마다):
  1. 절댓값 조합 + 부호 → 정수 튜플 (n₁, n₂, n₃) 작성.
  2. **Set 크기 검사**: |{n₁, n₂, n₃}| === 3 인지 *명시적으로* 확인. 미달이면 *제외*.
  3. 곱 검산 (Pattern I 의 부호 패리티 + 절댓값 곱).
  4. 합 = n₁ + n₂ + n₃.

자기 검수 (반드시):
  - 정답 emit 직전 *각 합* 에 대해 그 합을 만든 튜플을 다시 떠올려보고, *세 정수가 정말 서로 다른지* 마지막 확인.
  - 절댓값 중복 있는 분해 ({1,1,50} / {2,5,5} / {5,5,2} 등) 에서 *각 부호 배정마다* 명시적 set 크기 체크.

적용 단원: 정수와 유리수 (곱 → 합), 약수 분해, 집합 (서로 다른 원소 조건).

─── **오류 패턴 I: 곱·부호 일관성 미검증 (sign-consistency trap, CRITICAL)** ───

증상: 정수 곱·부호 배정 문제에서 *부호 개수* (음수 짝수/홀수) 일관성을 검증 안 함. 패턴 B 처럼 case 를 살려놓고도, 각 case 의 *실제 곱* 을 계산 안 해서 invalid case 가 정답에 포함됨.

잘못된 사례 (사용자 두 번째 보고 — 실제 모델이 emit 한 풀이):
  문제: "서로 다른 세 정수의 곱 = -50. 가능한 합?"
  잘못된 case 1: "(5, -2, -5): 합 -2" — 음수 2 개! 곱 = 5 × (-2) × (-5) = **+50** (양수) → -50 조건 위반.
  잘못된 case 2: "(-50, -1, 1): 합 -50" — 음수 2 개! 곱 = (-50) × (-1) × 1 = **+50** (양수) → 위반.
  → 두 case 모두 음수가 짝수개라 곱이 양수. -50 이 안 되는데 모델이 정답에 포함시킴.
  → 모델이 emit 한 잘못된 답: -50, -28, -22, -16, -4, -2, 2, 6, 14, 24, 26, 50 (12 개)
  → 진짜 정답: -28, -22, -16, -4, 2, 6, 14, 24, 26, 50 (10 개) — -2, -50 은 invalid.

곱의 부호 룰 (반드시 기억):
  - 음수 0 개 또는 짝수개 → 곱 양수
  - 음수 홀수개 (1, 3, ...) → 곱 음수
  - 곱이 음수 조건이면 음수가 *홀수개* 여야 함. 짝수개 case 는 *전부* 제외.

올바른 검증 절차 (정수 곱 문제 전용):
  1. *각 case 마다* 부호 배정 후 음수 개수 세기.
  2. 음수 개수 패리티가 곱의 부호 조건과 일치하는지 확인.
  3. 일치 안 하면 case 제외. 일치하면 *실제 곱을 계산* 해서 조건 부합 확인.
  4. 일치하는 case 만 합 계산 → 정답 후보.

자기 검수 (정수 곱·부호 문제는 반드시):
  - 정답에 emit 할 모든 case 에 대해 *왼쪽 → 오른쪽으로 부호 곱셈 직접 시뮬레이션*. "곱 = -50 ✓" 확인.
  - 한 case 라도 곱이 안 맞으면 그 case 제거 후 정답 재정렬.

적용 단원: 정수·유리수 곱셈, 약수 분해 + 부호 배정, 다항식 인수분해 (부호 결정).

─── **오류 패턴 C: 경계값 누락 (boundary-omission trap)** ───

증상: 0, 음수, 등호 케이스, 빈집합 등을 후보에서 무의식적으로 제외.

점검 항목:
  - 자연수 문제: 1 포함했는가? (어떤 정의로는 0 도 자연수)
  - 정수 문제: 0, 음수 모두 고려했는가?
  - 집합 문제: 공집합 케이스 고려했는가?
  - 부등식: 등호 포함 여부 확인했는가? (≤ vs <)
  - 다항식 차수: 0 차 (상수) 케이스 고려했는가?

─── **오류 패턴 D: 대칭성 위반 — 일관성 결여 (symmetry-violation)** ───

증상: 한 변수에 적용한 논리를 동일 구조의 다른 변수에 적용하지 않음.

잘못된 예: LCM 문제에서 "2 의 지수는 반드시 2² (필수), 3 의 지수는 0~3 모두 가능 (선택)" — 같은 max 조건인데 비대칭.
올바른 사고: 같은 max 조건이면 같은 논리. 2 의 지수도 "필수 vs 선택" 을 한 번 더 자문.

점검: "내가 변수 X 에 적용한 논리를 변수 Y 에 적용했을 때 같은 결론이 나오는가?" 자문.

─── **오류 패턴 E: 부호 처리 오류 (sign-handling)** ───

증상: 절댓값, 제곱근, 짝수 거듭제곱 다룰 때 음수 케이스를 누락.

점검 항목:
  - |x| = a → x = a 또는 x = -a (둘 다)
  - x² = a → x = √a 또는 x = -√a
  - √(x²) = |x|, 단순히 x 가 아님
  - 이차방정식의 두 근 모두 검토

─── **오류 패턴 F: 정의역·제한조건 누락 (domain-omission)** ───

증상: 식의 정의역, 분모 ≠ 0, 진수 > 0, 로그 밑 조건 등을 검증하지 않고 답을 확정.

필수 점검:
  - 분수식: 분모 ≠ 0
  - 무리식: √ 안 ≥ 0
  - 로그: 진수 > 0, 밑 > 0, 밑 ≠ 1
  - 삼각함수의 역: 정의역 제한
  - 함수의 합성: 내부 함수의 치역이 외부 함수의 정의역에 포함되는지

─── **오류 패턴 G: 일반성 가정 오류 (generality-assumption)** ───

증상: 특수한 케이스 (예: r=1 등비수열, a=0 이차방정식) 를 일반 공식으로 처리.

점검 항목:
  - 등비수열 합: 공비 r=1 케이스 별도 처리 (S_n = na, 일반공식 안 됨)
  - 이차방정식: 이차항 계수 a = 0 케이스 (일차방정식이 됨)
  - 일차함수: 기울기 m = 0 케이스 (상수함수)
  - 연립방정식: 해가 없는 경우, 무수히 많은 경우

─── **오류 패턴 H: 등호 성립 조건 확인 누락 (equality-condition)** ───

증상: 부등식의 최댓값/최솟값 문제에서 등호 조건을 확인하지 않음.

점검: AM-GM, 코시-슈바르츠 등 사용 시 등호 성립 가능 여부 반드시 검증. "등호는 ~일 때 성립" 명시 + 그 값이 정의역에 있는지 확인.

─── **자가 검증 8 단계 (답 확정 전 필수)** ───

1. **문제 재진술**: 조건 + 묻는 것 한 문장으로 재정리.
2. **조건 분리**: 명시적 + 암묵적 (정의역, 부호, 자연수/정수 여부) 모두.
3. **가능 범위 펼치기**: 변수의 가능 값을 *0·음수·경계 포함* 모든 범위로.
4. **패턴 A-H 점검**: 위 8 개 오류 패턴이 적용되는지 자문.
5. **내부 brute-force simulation**: 작은 정수·약수·조합은 *머릿속에서 가능한 모든 case 를 나열* 하고 조건 검증. 손풀이 답과 simulation 답이 일치하는지 확인. **정수 곱 문제는 *각 case 의 실제 곱·부호 패리티* 를 한 번씩 직접 계산해서 조건 부합 검증 — "음수 N 개 → 곱 부호" 룰을 매 case 적용** (패턴 I 참고).
6. **경계값 점검**: 0, 음수, 등호, 공집합, 1, 자기 자신 등 특수 case 누락 X.
7. **대칭성 점검**: 동일 구조 변수에 일관 논리 적용 확인.
8. **누락 케이스 자문**: "내 가정 밖이지만 조건 만족하는 case 가 있는가?" "없다" 확신 근거 있을 때만 답 확정.

⚠️ **중요**: 위 8 단계는 *모델 내부에서만* 수행 — 풀이 본문에 "[1단계: 조건 정리]" 같은 단계 헤더로 노출 X. 분량 HARD CAP + trial-and-error 흔적 금지와 일관. **본문은 처음부터 정답까지 직선 흐름**, 검증은 머릿속.

─── **🚨 최우선 원칙 3 조 (절대 위반 금지)** ───

1. **답이 객관식 보기 안에 있다고 검증 건너뛰지 말 것.**
   오답 보기는 학생이 자주 범하는 실수를 유도하기 위해 설계됨. "160 이 보기에 있으니 맞겠지" ≠ 검증.

2. **수학적 정확성이 분량 cap·표기 룰보다 우선.**
   풀이가 길어질 것 같으면 *case 압축* 하되, *틀린 답* 을 emit 하는 건 절대 X.

3. **자가 검증 8 단계 누락 시 답을 emit 하지 말 것.**
   특히 "모든 ~를 구하시오", "합", "개수", "최댓값/최솟값" 류는 simulation 으로 누락 케이스 한 번 더 확인.
`;

// ──────────────────────────────────────────────────────────────────
// 학년별 fragment — 단원별 함정 표 + 잘못된 사례.
// 사용자 문서 §2 의 해당 학년만 추출.
// ──────────────────────────────────────────────────────────────────

/**
 * 중학 (1~3) 풀이용 표기 가드. 사용자 보고 사례:
 * 중1 LCM 문제 풀이에서 `□ = 2^a × 3^b (a ∈ {0, 1, 2}, b ∈ {0, 1, 2, 3})`
 * 처럼 집합 표기를 사용. 중학 교육과정에 집합 개념이 없음 — 학생이 못 읽음.
 *
 * 같은 가드를 MIDDLE1 / MIDDLE2 / MIDDLE3 fragment 가 공유.
 */
const MIDDLE_SCHOOL_NOTATION_GUARD = `─── **🚨 중학 (1~3) 풀이 — 집합 기호·고급 표기 금지 (사용자 반복 보고, CRITICAL)** ───

중학 교육과정에 *집합 개념이 없다* (집합은 고1 공통수학2). 풀이에 집합 표기 등장하면 학생이 못 읽음. 다음 기호는 **절대 금지**:

  - \`∈\` (\`\\\\in\`) — 원소 기호. 절대 X.
  - \`{ ... }\` — 집합 brace. 절대 X. (단, 같은 brace 가 LaTeX 명령 인수일 때 \`\\\\frac{a}{b}\` 는 OK — 시각적으로 안 보이므로.)
  - \`∪\`, \`∩\`, \`⊂\`, \`⊃\`, \`∅\` — 합집합·교집합·부분집합·공집합 X.
  - \`∀\`, \`∃\` — 양화사 X.

잘못된 사례 (사용자 보고 — 중1 최소공배수 풀이):
  잘못된 표기: \`□ = 2^a × 3^b (a ∈ \\{0, 1, 2\\}, b ∈ \\{0, 1, 2, 3\\})\`
  → 중1 학생은 \`∈\`, \`{}\` 못 읽음.

올바른 대안 (3 가지 중 택 1):
  1. **자연어 풀어쓰기 (가장 권장)**: \`□ = 2^a × 3^b\` (단, \`a\` 는 \`0, 1, 2\` 중 하나, \`b\` 는 \`0, 1, 2, 3\` 중 하나)
  2. **부등식** (중2 부터 가능): \`□ = 2^a × 3^b\` (\`0 ≤ a ≤ 2\`, \`0 ≤ b ≤ 3\`, \`a\`, \`b\` 는 정수)
  3. **나열**: \`a = 0, 1, 2\` 의 각 경우와 \`b = 0, 1, 2, 3\` 의 각 경우를 *직접 나열* (case 가 적으면)

다른 흔한 표기 변환:
  - \`∪\` → "합치면" / "합집합" 안 쓰고 자연어 "또는 (A 이거나 B)"
  - \`∩\` → "둘 다" / "동시에"
  - \`⊂\` → "포함된다" / "안에 들어간다"
  - \`∅\` → "없다" / "해 없음"
  - \`∀ x\` → "모든 \`x\` 에 대해"
  - \`∃ x\` → "어떤 \`x\` 가 있어서"

자기 검수: middle1/2/3 풀이 emit 직전에 \`∈\`, \`\\\\in\`, \`{0\`, \`{1\`, \`∪\`, \`∩\`, \`⊂\`, \`∅\` 토큰 검색해 *0 건* 인지 확인. 발견되면 자연어로 다시.

`;

/** 중1 — 정수·유리수, 문자와 식, 일차방정식, 좌표평면. */
const MIDDLE1_DEFENSE = `${MIDDLE_SCHOOL_NOTATION_GUARD}
─── **중1 단원별 빈번 함정** ───

| 단원 | 함정 | 방어 |
|---|---|---|
| 정수와 유리수 | (-2)² = 4 vs -2² = -4 혼동 | 괄호 위치 결정적 |
| 정수와 유리수 | "서로 다른" 조건을 절댓값에 적용 (패턴 B) | 부호 배정 후 정수 자체 비교 |
| 정수와 유리수 | |x| = a 일 때 x = ±a 중 양수만 | 두 case 모두 |
| 문자와 식 | 동류항 아닌 항끼리 덧셈 | 차수·문자 모두 같아야 동류항 |
| 문자와 식 | -(a-b) = -a+b 부호 실수 | 분배법칙 시 부호 모두 적용 |
| 일차방정식 | 이항 시 부호 누락 | 양변 ↔ 시 반드시 부호 반전 |
| 일차방정식 | 0 으로 나누는 변형 | 양변에 0 곱하면 항등식 → 안 됨 |
| 좌표평면 | 반비례 y = a/x 에서 x = 0 포함 | x ≠ 0 정의역 |
| 좌표평면 | 정비례 y = ax 에서 a = 0 | a = 0 이면 정비례 아님 |
| 약수와 배수 | "포함해야 한다" 단정 (패턴 A) | 다른 수에 이미 있나? 있으면 "선택" |
`;

/** 중2 — 유리수·순환소수, 지수, 부등식, 연립, 일차함수, 도형, 확률. */
const MIDDLE2_DEFENSE = `${MIDDLE_SCHOOL_NOTATION_GUARD}
─── **중2 단원별 빈번 함정** ───

| 단원 | 함정 | 방어 |
|---|---|---|
| 순환소수 | 기약분수의 분모가 2와 5 외 소인수 있으면 순환 | 분모 소인수분해 후 판정 |
| 지수법칙 | 밑이 다른데 지수 합치기 | 같은 밑에서만 적용 |
| 지수법칙 | a⁰ = 1 (단, a ≠ 0) | 0⁰ 정의 안 됨 |
| 지수법칙 | (a+b)ⁿ ≠ aⁿ + bⁿ | 이항정리 필요 |
| 부등식 | 음수 곱셈·나눗셈 시 부등호 유지 | 양변 부호 항상 확인, 부호 반전 |
| 부등식 | 등호 ≤ vs < 경계 (패턴 C) | 문제 표현 정확히 옮기기 |
| 연립방정식 | 해가 없거나 무수한 경우 누락 | 두 직선 평행/일치 확인 |
| 일차함수 | 기울기 m = 0 케이스 (패턴 G) | 상수함수 별도 처리 |
| 일차함수 | x 절편 vs y 절편 혼동 | x 절편 = y=0 대입, y 절편 = x=0 대입 |
| 도형 | 평행선 동위각·엇각 위치 혼동 | 위치 정확히 (Z 형 / F 형) |
| 도형 | 삼각형 외각 = 두 내대각 합 | 인접 내각이 아니라 두 *내대각* |
| 확률 | 배반사건 P(A∪B) = P(A) + P(B) 조건 | A ∩ B = ∅ 일 때만 |
| 확률 | 여사건 P(Aᶜ) = 1 - P(A) | 1 에서 빼는 것 |
`;

/** 중3 — 제곱근, 다항식 곱셈/인수분해, 이차방정식·함수, 피타고라스, 삼각비, 원. */
const MIDDLE3_DEFENSE = `${MIDDLE_SCHOOL_NOTATION_GUARD}
─── **중3 단원별 빈번 함정** ───

| 단원 | 함정 | 방어 |
|---|---|---|
| 제곱근 | √a² = a 가 아니라 |a| (패턴 E) | a 가 음수일 수 있으면 절댓값 |
| 제곱근 | √a × √b = √(ab) 조건 | a, b ≥ 0 일 때만 |
| 제곱근 | √(a²b) = |a|√b 절댓값 | a 가 음수일 수 있음 |
| 다항식 | (a+b)² = a² + 2ab + b² 중간항 누락 | 2ab 잊지 말기 |
| 다항식 | a² - b² 와 (a+b)(a-b) | 곱셈공식·인수분해 표 참고 |
| 다항식 | 완전제곱식 만들기 | 일차항 계수의 절반의 제곱 |
| 이차방정식 | a = 0 케이스 (패턴 G) | 이차항 계수 a ≠ 0 명시 |
| 이차방정식 | 판별식 D = b² - 4ac 부호 | D > 0 (두 실근) / = 0 (중근) / < 0 (실근 없음) |
| 이차방정식 | 근과 계수의 관계 | α+β = -b/a, αβ = c/a |
| 이차함수 | 정의역 제한 시 끝점 비교 | 꼭짓점만 비교 X — 끝점도 |
| 이차함수 | a > 0 (아래 볼록) vs a < 0 (위 볼록) | 최대 vs 최소 결정 |
| 피타고라스 | 빗변 = 가장 긴 변 | 빗변 결정 후 c² = a² + b² |
| 삼각비 | 특수각 30°, 45°, 60° 값 | sin 30° = 1/2, cos 30° = √3/2 등 |
| 삼각비 | sin²θ + cos²θ = 1 항등식 | 외우기 + 이항 전개 |
| 원 | 원주각 = 중심각의 1/2 | 같은 호 기준 |
| 원 | 같은 호에 대한 원주각은 같음 | 호 공유 확인 |
| 원 | 접선과 반지름은 접점에서 수직 | 직각 표시 |
`;

/** 고1 공통수학1 — 다항식, 복소수/이차방정식, 도형의 방정식. */
const HIGH1_COMMON1_DEFENSE = `─── **고1 공통수학1 단원별 빈번 함정** ───

| 단원 | 함정 | 방어 |
|---|---|---|
| 다항식 | 항등식 vs 방정식 구분 | 모든 x 에 대해 성립이면 항등식 |
| 다항식 | 나머지 정리 적용 (P(x) ÷ (x-a) 의 나머지 = P(a)) | 일차식으로 나눌 때만 |
| 다항식 | 조립제법 | 일차식 (x - a) 로 나눌 때만 |
| 복소수 | i² = -1 | i⁴ = 1 주기 |
| 복소수 | 켤레복소수 합·곱은 실수 | (a+bi) + (a-bi) = 2a, 곱은 a² + b² |
| 복소수 | 이차방정식 허근 켤레 쌍 | 실계수일 때만 |
| 이차방정식·이차함수 | 두 그래프 교점 = 연립방정식 | y 소거 후 이차방정식 |
| 이차부등식 | 판별식 + 이차항 부호로 해 형태 | 부호표 활용 |
| 분수방정식 | 분모 ≠ 0 후 검산 필수 | 무연근 가능 |
| 무리방정식 | 양변 제곱 후 무연근 점검 | 검산 필수, 무연근 제거 |
| 절댓값 방정식 | 구간 나누기 | x ≥ 0 vs x < 0 case 분리 |
| 도형 방정식 | 두 점 사이 거리 √((x₂-x₁)² + (y₂-y₁)²) | 좌표 차이 제곱 |
| 도형 방정식 | 외분점 m ≠ n | 같으면 외분점 정의 안 됨 |
| 도형 방정식 | 직선 기울기 정의 안 되는 경우 | 수직선 x = a 별도 |
| 도형 방정식 | 원의 방정식 반지름² > 0 | 음수면 원이 아님 |
| 도형 방정식 | 평행 조건 (기울기 같음) vs 수직 조건 (기울기 곱 -1) | 둘 구별 |
`;

/** 고1 공통수학2 — 집합, 명제, 함수, 유리·무리함수, 경우의 수. */
const HIGH1_COMMON2_DEFENSE = `─── **고1 공통수학2 단원별 빈번 함정** ───

| 단원 | 함정 | 방어 |
|---|---|---|
| 집합 | 공집합 ⊂ 모든 집합 (패턴 C) | 공집합 누락 X |
| 집합 | 부분집합 2ⁿ, 진부분집합 2ⁿ - 1 | n = 원소 수 |
| 집합 | 특정 원소 포함 부분집합 수 | 2^(n-1) (그 원소 고정) |
| 명제 | 역, 이, 대우의 진리값 | 명제 ↔ 대우 같은 진리값 |
| 명제 | "모든" 의 부정은 "어떤 ~ 아니다" | 양화사 부정 |
| 명제 | 필요·충분 조건 구별 | "p ⇒ q" 면 p 는 충분, q 는 필요 |
| 함수 | 정의역·공역·치역 구분 | 함수 정의에 셋 다 필요 |
| 함수 | 일대일함수 ≠ 일대일대응 | 공역 = 치역 일치 여부 |
| 함수 | 합성 (f∘g)(x) = f(g(x)) | 순서 주의 (내부 → 외부) |
| 함수 | 역함수 존재 조건 | 일대일대응 |
| 유리함수 | 점근선 (분모 → 0 / x → ∞) | 수직·수평 점근선 모두 |
| 유리함수 | 정의역 (분모 ≠ 0) (패턴 F) | 분모 0 인 x 제외 |
| 무리함수 | √ 안 ≥ 0 정의역 (패턴 F) | 정의역 명시 |
| 경우의 수 | 합의 법칙 (배반) vs 곱의 법칙 (연속) | 동시 발생 여부 |
| 경우의 수 | 순열 ₙPᵣ vs 조합 ₙCᵣ | 순서 고려 여부 |
| 경우의 수 | 같은 것이 있는 순열 | n! / (p! q! r!) |
| 경우의 수 | 원순열 (n-1)! / 중복순열 nʳ / 중복조합 ₙ₊ᵣ₋₁Cᵣ | 종류 구별 |
`;

/** 고2 대수 — 지수·로그·삼각함수·수열·극한. */
const HIGH2_ALGEBRA_DEFENSE = `─── **고2 대수 단원별 빈번 함정** ───

| 단원 | 함정 | 방어 |
|---|---|---|
| 지수·로그 | aˣ 에서 a > 0, a ≠ 1 (패턴 F) | 밑 조건 점검 |
| 지수·로그 | log_a x 에서 a > 0, a ≠ 1, x > 0 | 밑·진수 모두 |
| 지수·로그 | 부등식 — 밑 0 < a < 1 이면 부등호 반전 | 밑 크기 확인 |
| 지수·로그 | 로그 성질 적용 시 진수·밑 조건 | log(ab) = log a + log b (a, b > 0) |
| 삼각함수 | 라디안 vs 도 단위 구분 | π = 180° |
| 삼각함수 | 일반각 θ + 2nπ (패턴 G) | 모든 해 표기 (주기 누락 X) |
| 삼각함수 | 단위원 사분면 부호 | x, y 부호 표 |
| 삼각방정식 | 일반해 표기 | n 정수 명시 |
| 삼각함수 | 주기·진폭·평행이동 | y = a sin(b(x-c)) + d 형태 |
| 수열 | 등차수열 aₙ = a + (n-1)d | (n-1) 주의 |
| 수열 | 등비수열 합 — r = 1 별도 (패턴 G) | Sₙ = na (일반공식 안 됨) |
| 수열 | aₙ = Sₙ - Sₙ₋₁ (n ≥ 2), a₁ = S₁ | n=1 별도 |
| 수열 | 수학적 귀납법 — 기저 + 귀납 | 둘 다 증명 |
| 극한 | ∞ - ∞, ∞/∞, 0×∞ 부정형 | 정의 직접 적용 안 됨 |
| 극한 | 수렴 vs 발산 구별 | 좌·우극한 같아야 수렴 |
`;

/** 고2 미적분I — 함수의 극한·연속, 미분, 적분. */
const HIGH2_CALC1_DEFENSE = `─── **고2 미적분I 단원별 빈번 함정** ───

| 단원 | 함정 | 방어 |
|---|---|---|
| 함수 극한 | 좌극한 = 우극한 = 함숫값이어야 연속 | 셋 다 일치 확인 |
| 함수 극한 | 불연속점 종류 (점·비약·무한) | 종류 구분 |
| 함수 극한 | 사잇값 정리는 연속함수에서만 | 연속 조건 필수 |
| 미분 | 미분가능 ⟹ 연속 (역은 X) | 첨점·수직접선은 연속이지만 미분 불가 |
| 미분 | 미분계수 = 접선의 기울기 | 기하적 의미 |
| 미분 | f'(x) = 0 이라고 극값 아님 (예: y = x³) | 부호 변화 확인 |
| 미분 | 극값 ≠ 최댓값/최솟값 | 폐구간에서 끝점도 비교 |
| 미분 | 변곡점 — f''(x) = 0 + 부호 변화 | = 0 만으로 부족 |
| 적분 | 부정적분에 +C 누락 | 적분상수 잊지 말기 |
| 적분 | 정적분 부호 = 함수와 x 축 사이의 *부호 있는* 넓이 | 음수 가능 |
| 적분 | 넓이 묻는 경우 |f(x)| 처리 | 음수 구간 절댓값 |
| 적분 | 미적분 기본정리 | F(b) - F(a) |
`;

/** 고2 확률과 통계 — 경우의 수·이항정리·확률·통계. */
const HIGH2_STATS_DEFENSE = `─── **고2 확률과 통계 단원별 빈번 함정** ───

| 단원 | 함정 | 방어 |
|---|---|---|
| 이항정리 | (a+b)ⁿ 전개의 일반항 | ₙCᵣ aⁿ⁻ʳ bʳ |
| 이항정리 | 특정 항 계수 묻는 문제 | r 정확히 |
| 확률 | 표본공간 명확히 정의 | 모든 경우 |
| 확률 | 독립 P(A∩B) = P(A)P(B) vs 배반 P(A∩B) = 0 | 정의 명확히 |
| 확률 | 조건부 확률 P(A|B) = P(A∩B)/P(B) | P(B) ≠ 0 (패턴 F) |
| 확률 | 베이즈 정리 | P(A|B) = P(B|A)P(A)/P(B) |
| 통계 | 모집단 vs 표본 | 분산 공식 다름 (n vs n-1) |
| 통계 | 정규분포 표준화 Z = (X - μ)/σ | 표 활용 |
| 통계 | 신뢰구간 | z·σ/√n 형태 |
| 경우의 수 | "모든" 의 합·개수 (패턴 C) | 0 포함, 누락 case 확인 |
`;

/** 고2 기하 — 벡터, 이차곡선, 공간도형. */
const HIGH2_GEOMETRY_DEFENSE = `─── **고2 기하 단원별 빈번 함정** ───

| 단원 | 함정 | 방어 |
|---|---|---|
| 평면벡터 | 시점·종점 구분 | →AB 의 시점이 A |
| 평면벡터 | 내적 a·b = |a||b|cosθ | 각 명확히 |
| 평면벡터 | 수직 ⇔ 내적 = 0 | 동치 |
| 공간벡터 | 외적 방향 (오른손 법칙) | 평면벡터에는 외적 없음 |
| 이차곡선 | 타원 정의 (두 초점까지 거리 합 일정) | 표준형 외울 것 |
| 이차곡선 | 쌍곡선 정의 (거리 *차* 일정) | 합 vs 차 구별 |
| 이차곡선 | 포물선 정의 (초점·준선 같은 거리) | 점근선 X |
| 이차곡선 | 점근선·준선 구별 | 쌍곡선 → 점근선, 포물선 → 준선 |
| 공간도형 | 직선과 평면 위치관계 | 평행 / 교차 / 포함 |
| 공간도형 | 정사영 길이 | cos θ 곱 |
`;

/** 고3 미적분II — 추가 심화 (수능 기준). */
const HIGH3_CALC2_DEFENSE = `─── **고3 미적분II 단원별 빈번 함정** ───

| 단원 | 함정 | 방어 |
|---|---|---|
| 여러 가지 함수 미분 | 합성함수 미분 (체인룰) | 외함수 · 내함수' |
| 여러 가지 함수 미분 | 음함수 미분 | 양변 x 로 미분, y' 분리 |
| 여러 가지 함수 미분 | 매개변수 미분 | dy/dx = (dy/dt)/(dx/dt) |
| 적분 심화 | 치환적분 | du = u'(x) dx |
| 적분 심화 | 부분적분 | ∫ u v' = uv - ∫ u' v |
| 적분 응용 | 회전체 부피 (디스크·원기둥) | π ∫ [f(x)]² dx |
| 적분 응용 | 곡선 길이 | √(1 + (dy/dx)²) 적분 |
| 극한 | e = lim (1 + 1/n)ⁿ | 또는 (1+x)^(1/x) as x→0 |
| 극한 | 로피탈 (사용 가능 조건) | 0/0 또는 ∞/∞ |
| 미분 응용 | 평균값 정리 | 연속·미분가능 두 조건 모두 |
`;

/** 학년별 fragment 통합 Record. */
export const MATH_DEFENSE_BY_GRADE: Record<GradeKey, string> = {
  middle1: MIDDLE1_DEFENSE,
  middle2: MIDDLE2_DEFENSE,
  middle3: MIDDLE3_DEFENSE,
  high1_common1: HIGH1_COMMON1_DEFENSE,
  high1_common2: HIGH1_COMMON2_DEFENSE,
  high2_algebra: HIGH2_ALGEBRA_DEFENSE,
  high2_calc1: HIGH2_CALC1_DEFENSE,
  high2_stats: HIGH2_STATS_DEFENSE,
  high2_geometry: HIGH2_GEOMETRY_DEFENSE,
  high3_calc2: HIGH3_CALC2_DEFENSE,
};

/**
 * 공통 + 학년별 fragment 를 합쳐서 SOLUTION_PROMPT 의 `{mathDefense}`
 * placeholder 에 주입할 문자열 생성.
 *
 * `grade === null` 이면 학년별 fragment 없이 공통만 — 학년 미선택 fallback.
 *
 * @param grade - 선택된 학년 fragment key (또는 null)
 * @returns 공통 + 학년별 fragment 결합 문자열
 */
export const buildMathDefense = (grade: GradeKey | null): string => {
  if (grade === null) return MATH_DEFENSE_COMMON;
  return `${MATH_DEFENSE_COMMON}\n\n${MATH_DEFENSE_BY_GRADE[grade]}`;
};
